---
layout: post
title: Installation of PyTorch and Common Tools
subtitle: 安装pytorch及常用工具
date: 2024-11-30T00:00:00.000Z
author: Byolio
header-img: img/Pytorch.jpg
catalog: true
tags:
  - AI
  - PyTorch
  - python
lang: en
---

> 🌐 [中文](https://www.byolio.blog/2024-11-30-torch/) | [English](https://www.byolio.blog/en/en/_posts/2024-11-30-torch/)

> This article aims to introduce the installation of PyTorch and its common tools. (Please keep the magic enabled throughout to solve network issues)

## What is PyTorch
PyTorch is an open-source deep learning framework developed by `Facebook`'s AI Research lab (FAIR). It is known for its dynamic computation graph, intuitive API, and powerful GPU acceleration capabilities. PyTorch is mainly used in fields such as computer vision and natural language processing, enabling researchers and developers to quickly build and train neural network models.
## Why Choose PyTorch
1. Dynamic Computation Graph: PyTorch adopts a dynamic computation graph mechanism, allowing changes to the network structure at runtime, facilitating debugging and development.
2. Ease of Use: PyTorch's API design is intuitive and close to Python's coding style, lowering the learning curve.
3. Strong Community Support: It has an active community and a large number of third-party libraries, making the development process more convenient.
4. High Extensibility: Supports custom layers and operations, making development easier.
## PyTorch Installation (Taking Windows as an Example)
1. Install CUDA (GPU version of PyTorch requires CUDA)
Open the terminal and enter:
```bash
nvidia-smi
```
Check the driver version. The [official CUDA documentation](https://docs.nvidia.com/deploy/cuda-compatibility/) lists the minimum driver version requirements for each toolkit version. For example, `CUDA 12.7` typically requires driver version `525.85.12` or higher.
![cudaVision](https://cdn.jsdelivr.net/gh/byolio/newtc@main/img/cudaVision.png)
Download the CUDA driver [link](https://developer.nvidia.com/cuda-downloads?target_os=Windows&target_arch=x86_64&target_version=11&target_type=exe_local) (I have already selected it, just download).
![CUDA](https://cdn.jsdelivr.net/gh/byolio/newtc@main/img/CUDA.png) Restart the computer after installation.
After restarting, open the terminal and enter:
```bash
nvcc -V
``` 
If CUDA-related information is displayed, the installation is successful.
2. Install PyTorch
Open the [PyTorch official download page](https://pytorch.org/get-started/locally/)
![pytorchDownload](https://cdn.jsdelivr.net/gh/byolio/newtc@main/img/pytorchDownload.png) Select the corresponding command and copy it to the terminal to download (it is not recommended to download to the base environment).
## Installation of Common PyTorch Tools
I use conda for installation here. [Method to install conda](https://byolio.top/2024/10/03/conda/).
1. Jupyter (Simple introduction; I won't write advanced Jupyter content)
Conda comes with Jupyter. I mainly explain how to create a virtual environment with PyTorch installed and import that virtual environment into Jupyter. \
Use the following code (note to use your own Python version; copy `python -V` to the terminal to check your Python version):
```bash
conda create -n pytorch python=3.10
conda activate pytorch
```
Then install PyTorch in this environment using the previously used PyTorch installation command. Restart the terminal after installation.
Create the corresponding IPython kernel:
```bash
python -m ipykernel install --user --name=pytorch --display-name "Pytorch_env"
```
Start Jupyter:
```bash
jupyter notebook
```
You will see the newly added Jupyter kernel.
2. Tensorboard (Optional)
Switch to the environment with `PyTorch` and install:
```bash
conda install tensorboard
```
Start Tensorboard:
```bash
tensorboard --logdir=logs
```
`logs` is the folder for saving logs; please replace it accordingly.
3. Pycharm
Basic IDE tool; no further explanation needed. Just [download and install](https://www.jetbrains.com/pycharm/download/?section=windows). Advanced content will be written later. \
Note that the interpreter must be the environment with `PyTorch`.
You can test with `import torch`.

## FAQ
1. If the conda command cannot be used
Check that the correct environment variables are set, then enter in the terminal:
```bash
conda init
```
2. How to test if GPU is available
```python
import torch
print(torch.cuda.is_available())
```
If it returns True, it means GPU is available.
3. How to test GPU computing capability
```python
import torch
print(torch.cuda.get_device_capability())
```

## Summary
The above is the installation method for PyTorch. I hope it helps everyone.

